<form method="post" action="{{ path('partie.nouvelle-table', {'id': player.id}) }}" style="text-align: center; margin-bottom: 5px;">
    <input type="hidden" name="_token" value="{{ csrf_token('nouvelleTable' ~ player.id) }}">
    <button id="acref">Ouvrir une nouvelle table</button>
</form>
