{% extends 'base.html.twig' %}

{% block title %}CyberNative - Modifier Profil{% endblock %}

{% block css %}    <link href="{{ "css/comptejoueur.css" }}" rel="stylesheet">
{% endblock %}

{% block body %}
    <div>
        <div id="joueur">
            {{ form_start(form) }}
            <div class="row">
                <div class="form-group col-xs-6">
                    {{ form_row(form.username) }}
                </div>
                <div class="form-group col-xs-6">
                    {{ form_row(form.password) }}
                </div>
            </div>
            <button class="acref">{{ button_label|default('Enregistrer') }}</button>
            {{ form_end(form) }}
        </div>
    </div>



{% endblock %}
